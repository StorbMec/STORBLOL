/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:true,
    images : { 
        domains : ['cdnb.artstation.com', 'dotesports.com'] 
      } 
};

export default nextConfig;
